"""
RAG Agent — knowledge-navigator

Pattern: Retrieval-Augmented Generation (RAG)

WHY RAG:
Rather than relying on an LLM's training data (which may be outdated or
hallucinated), RAG agents retrieve relevant document chunks first, then
generate answers grounded in those retrieved sources. Every answer includes
citations showing exactly which sections were used.

Implementation begins Phase 1, Week 5.
"""
# TODO Phase 1 Wk 5: implement RagAgent class
